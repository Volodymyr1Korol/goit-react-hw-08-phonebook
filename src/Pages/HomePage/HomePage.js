import { Typography } from '@mui/material';

export default function HomePage() {
  return (
    <Typography
      variant="h2"
      component="h1"
      padding="50px 24px 0"
      textAlign="center"
    >
      Welcome to page contactsApp
    </Typography>
  );
}
